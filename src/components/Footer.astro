---
import { getEntry } from "astro:content";

const settings = await getEntry("settings", "index");
---

<footer
  class="mx-auto mb-20 flex w-full max-w-7xl flex-col items-center justify-between gap-2 px-4 py-8 sm:flex-row sm:gap-8 sm:px-6 lg:px-8"
>
  <pre x-html="JSON.stringify($store.player, null, 2)"></pre>
  <p @woof="console.log('woof')"></p>

  <p>Copyright &copy; {new Date().getFullYear()} — The NavBar.</p>
  <p class="flex items-center justify-center gap-4">
    <span>Tweeeeet at us:</span>
    <a
      href={`https://twitter.com/${settings.data.twitterHandle}`}
      class="inline-block"
    >
      <svg
        xmlns="http://www.w3.org/2000/svg"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="2"
        stroke-linecap="round"
        stroke-linejoin="round"
        class="size-6 hover:fill-black"
        ><path
          d="M22 4s-.7 2.1-2 3.4c1.6 10-9.4 17.3-18 11.6 2.2.1 4.4-.6 6-2C3 15.5.5 9.6 3 5c2.2 2.6 5.6 4.1 9 4-.9-4.2 4-6.6 7-3.8 1.1 0 3-1.2 3-1.2z"
        ></path></svg
      >
    </a>
  </p>
</footer>
